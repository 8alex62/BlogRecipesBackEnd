components:
  schemas:
    LoginRequest:
      type: object
      properties:
        username:
          type: string
          description: The username for login
          example: "testuser"
        password:
          type: string
          description: The password for login
          example: "password123"
      required:
        - username
        - password